"""
Wallet Pydantic schemas — placeholder.

Wallet schemas are currently defined inline in app.wallet.router (BalanceOut,
TransactionOut, CreateOrderRequest, etc.). Move here if the module grows.
"""
