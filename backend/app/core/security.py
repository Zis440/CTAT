"""
Security utilities — placeholder.

Auth security is implemented in:
  - app.auth.jwt_utils (JWT + password hashing)
  - app.auth.dependencies (get_current_user, role guards)
  - app.auth.oauth (Google/Microsoft OAuth)

Add shared security utilities here (e.g., rate limiting, IP blocking).
"""
