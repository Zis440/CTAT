"""
Session ORM model — placeholder.

The Session model is co-located with Patient in app.models.patient to keep
related models together. Import from there:

    from app.models.patient import Session
"""
